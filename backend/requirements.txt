fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic[email]==2.7.1
python-multipart==0.0.9
httpx==0.27.0
python-dotenv==1.0.1

# Database (PostgreSQL - for production)
# sqlalchemy==2.0.30
# asyncpg==0.29.0
# alembic==1.13.1

# Payments
# stripe==9.12.0
# flutterwave (use their REST API directly with httpx)

# SMS (Africa's Talking)
# africastalking==1.2.7

# Redis (for real-time tracking)
# redis==5.0.4
